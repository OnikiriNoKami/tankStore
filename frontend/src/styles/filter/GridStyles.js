import { makeStyles } from "@material-ui/core";

const GridStyles = makeStyles((theme)=> {
    return {
        GridMarginBottom: {
            marginBottom: 10,
        }
    }
})

export default GridStyles