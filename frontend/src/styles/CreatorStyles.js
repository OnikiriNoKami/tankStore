import { makeStyles } from "@material-ui/core";

const CreatorStyles = makeStyles(theme => {

    return {
        root: {
            backgroundColor: 'white',
        }
    }
})

export default CreatorStyles